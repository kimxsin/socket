spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/petharmony  # ?? ??? DB
    username: petharmony  # DB ??? ??
    password: petharmony  # DB ????

  jpa:
    hibernate:
      ddl-auto: update  # ? ??? ???? ???? ???? (????? ??? ???? ??)
      database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true  # ??? SQL? ??? ??
  mybatis:
    mapper-locations: mappers/**/*.xml  # MyBatis ??
